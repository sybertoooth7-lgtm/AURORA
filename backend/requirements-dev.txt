# Pin the exact toolchain versions used for local verification so CI results
# are identical (older ruff 0.9.x flagged single-element isinstance tuples;
# those were already cleaned up, but a pinned toolchain removes any other
# cross-version drift between ruff >=1.0 and mypy 1.x vs 2.x).
ruff==0.16.7
mypy==2.3.1